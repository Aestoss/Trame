# Optional. Defaults shown.

# Port the server listens on.
PORT=3000

# Directory where the SQLite database (trame.db) and generated images are
# stored. On Railway, point this at a mounted volume (e.g. /data) so game
# data survives redeploys.
DATA_DIR=./data
